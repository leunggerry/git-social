<div class="main-background-image">
  <div class="container">
    <div class="section">
      <div class="content-wrap">
        {{#each posts as |post|}}
          {{> post-info post }}
        {{/each}}
      </div>
    </div>
  </div>
</div>