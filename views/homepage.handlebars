<div class="main-background-image">
  <div class="container">
    <div class="section">
      <div class="content-wrap">
        <div class="row">
        {{#each posts as |post|}}
          {{> post-info post }}
        {{/each}}
        </div>
      </div>
    </div>
  </div>
</div>